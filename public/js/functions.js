function pointing(e, mode, side) {
	// let text = document.querySelector('a-text');
	// text.setAttribute('value', `pointing ${mode} ${side}`);
}
function point(e, mode, side) {
	// let text = document.querySelector('a-text');
	// text.setAttribute('value', `point ${mode} ${side}`);
}
function grip(e, mode, side) {
	// let text = document.querySelector('a-text');
	// text.setAttribute('value', `grip ${mode} ${side}`);
}

function button(button) {
	// let text = document.querySelector('a-text');
	// text.setAttribute('value', `button ${button}`);
}
